include /etc/nginx/mime.types;

server {
  listen 80;
  # server_name localhost;
  server_name 0.0.0.0;
  root /var/www/site;
  include /etc/nginx/mime.types;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
